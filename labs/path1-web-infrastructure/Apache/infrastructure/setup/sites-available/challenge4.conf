<VirtualHost *:80>
    ServerName challenge4.local
    DocumentRoot /var/www/challenge4
    <Directory /var/www/challenge4>
        AllowOverride All
        Require all granted
    </Directory>
    # Add basic auth challenge, Your goal is to put this page behind a basic auth
    # When done you'll see the flag in the page
    # Ask the GPT for help if you are stuck
</VirtualHost>